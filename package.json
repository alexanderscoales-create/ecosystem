{
  "name": "ares-habitat",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "art": "node scripts/generate-art.mjs",
    "dev": "npm run art && vite",
    "build": "npm run art && tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit false --emitDeclarationOnly false"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
